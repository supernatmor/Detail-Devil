// const router = require("express").Router();
// const companyController = require("../controllers/companyController");
// // const userController = require("../controllers/userController.js");

// // Routes

// // Route to get info from company collection
// router.route("/companies").get(companyController.findAll);

// // Get companies names, price range, rating, address

// // When one chosen. Get company packages with descriptions, name, price
// router.route("companies/:id").get(companyController.findById);

// // When package chosen. Get time's for each package
// router.route("times/:id").get(companyController.findTimes);

// // Booking. Get customer info if logged in
// // router.route("user/:id").get(userController.currentUser);

// // User Sign UP. POST Info
// // router.route("/user/login").post(userController.newUser);


// module.exports = router;